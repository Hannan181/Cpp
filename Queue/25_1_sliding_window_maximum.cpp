#include<iostream>
#include<vector>
#include<deque>
using namespace std;
 
int main(){
   /*
   sliding window maximum
   given an array of elements
   wehave to print the largest of the any three set of elements
   */
  int n,k;
  cin>>n>>k;
  deque<int>d1;
  for(int i=0;i<n;i++){
      int temp;
      cin>>temp;
      d1.push_back(temp);
  }
int j=0;


return 0;
}